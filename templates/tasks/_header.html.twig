<nav class="main-header navbar navbar-expand navbar-text-dark bg-white pe-3">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item text-primary">
            <a class="nav-link" id="pushmenu" href="#"><i class="fas fa-bars"></i></a>
        </li>
    </ul>
    <!-- Right navbar links -->
    <ul class="navbar-nav ms-auto">
        <!-- Messages Dropdown Menu -->
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="true">
                {{ nzo_decrypt(app.user.firstName) ~' '~ nzo_decrypt(app.user.lastName) }}
            </a>
            <div class="dropdown-menu end-0">
                <a href="{{ path('admin_logout') }}" class="dropdown-item" style="min-width: 100px !important;">
                    <!-- Message Start -->
                    <div class="media">
                        <div class="media-body">
                            <p class="mb-0">
                                <i class="fa-light fa-arrow-up-left-from-circle"></i>
                                <span class="ps-3">Sign Out</span>
                            </p>
                        </div>
                    </div>
                    <!-- Message End -->
                </a>
            </div>
        </li>
    </ul>
</nav>